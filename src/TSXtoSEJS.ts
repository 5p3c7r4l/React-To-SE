const path = require('path');
const fs = require('node:fs');
const express = require('express')
const app = express()

export default function TSXToJsSE (Path:string='.',BuildMode:boolean=false):number {
    

    return 1;
}
